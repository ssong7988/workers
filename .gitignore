# Secrets and local configuration
.env
**/.env
**/data/kakao-token.json

# Python
**/.venv/
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.mypy_cache/

# Editors and operating systems
.vscode/
.idea/
.DS_Store
Thumbs.db

# Runtime data
**/logs/
**/*.log
.logs/
real-estate-finder/data/
real-estate-finder/config/searches.local.yaml
spending-analyzer/data/
spending-analyzer/config/settings.local.yaml

# report-site/ (Django). The database is PostgreSQL, outside the repo;
# collectstatic output and any scratch files are generated.
report-site/db.sqlite3
report-site/staticfiles/
report-site/data/

# dagster_project/ - DAGSTER_HOME (run history, its own SQLite metadata db)
dagster_project/data/

# Browser profiles left behind by screenshot tooling
**/edge-shot-profile*/

# stock-importer/ - browser dumps, run lock, collected JSON
stock-importer/data/

# 스크린샷·PDF 등 확인용 임시 산출물
tmp/
